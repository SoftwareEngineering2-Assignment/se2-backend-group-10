/**
 * imports the mongoose dependency and stores it in a variable
 * exports the mongoose variable so that other modules can use it
 */
const mongoose = require("./mongoose");

module.exports = { mongoose };
